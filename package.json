{
  "name": "shadcn-ui-monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --parallel",
    "lint": "turbo lint",
    "prepare": "pnpm install --ignore-scripts",
    "postinstall": "cd apps/ml && poetry install"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@workspace/typescript-config": "workspace:*",
    "turbo": "^2.3.0",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20"
  }
}
