{
  "name": "@repo/ml",
  "scripts": {
    "dev": "cd ml && poetry run uvicorn main:app --reload --port 5000",
    "build": "echo 'No build needed for Python project'",
    "lint": "poetry run pylint *.py",
    "test": "poetry run pytest tests/"
  }
}
