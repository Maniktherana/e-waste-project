{
  "name": "ml-api",
  "scripts": {
    "dev": "poetry run uvicorn main:app --reload --port 8000",
    "build": "echo 'No build needed for Python project'",
    "lint": "poetry run pylint main.py"
  }
}
